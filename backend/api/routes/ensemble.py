from fastapi import Request
from orchestration.ensembles.router import DynamicEnsembleRouter

# Add an endpoint to the existing router programmatically or we can just append it via replace_file_content to routes/__init__.py
